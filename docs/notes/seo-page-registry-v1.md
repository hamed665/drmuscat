# seo-page-registry-v1

This branch introduces a central SEO page registry and uses it to build the static sitemap entries.

The initial registry preserves the existing static sitemap coverage while creating one place for future indexability, readiness, and quality gates.
