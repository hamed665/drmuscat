import Link from "next/link";
import { FaqCmsForm } from "@/components/admin/cms/faq-cms-form";
import { approveAdminFaqRevision, archiveAdminFaqEntry, rejectAdminFaqRevision, restoreAdminFaqEntry, submitAdminFaqRevisionForReview } from "@/server/admin/cms-faq-actions";
import { getAdminFaqCmsEntry } from "@/server/admin/cms-faqs";

export default async function FaqCmsDetailPage({ params }: { params: Promise<{ entryId: string }> }) { const { entryId } = await params; const result = await getAdminFaqCmsEntry(entryId); if (!result.ok) return <p className="text-red-700">FAQ CMS entry could not be loaded.</p>; const item = result.item; return <div className="space-y-6"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-sm uppercase tracking-[0.2em] text-cyan-700">FAQ CMS entry</p><h2 className="text-2xl font-bold text-slate-950">{item.contentKey}</h2><p className="text-sm text-slate-600">Status: {item.status} · Revision: {item.currentRevisionNumber ?? "—"} · {item.isArchived ? "Archived" : "Active"}</p></div><div className="flex gap-2"><Link className="rounded-full border px-4 py-2 text-sm font-semibold" href="/admin/content/faqs">FAQ list</Link><Link className="rounded-full border px-4 py-2 text-sm font-semibold" href={`/admin/content/faqs/${entryId}/preview`}>Preview</Link></div></div><FaqCmsForm item={item}/><section className="rounded-2xl border bg-white p-5"><h3 className="font-bold">FAQ workflow actions</h3><div className="mt-3 flex flex-wrap gap-2"><form action={submitAdminFaqRevisionForReview}><input type="hidden" name="entryId" value={entryId}/><button className="rounded-full border px-3 py-2 text-sm font-semibold">Submit for review</button></form><form action={approveAdminFaqRevision}><input type="hidden" name="entryId" value={entryId}/><button className="rounded-full border px-3 py-2 text-sm font-semibold">Approve internally</button></form><form action={rejectAdminFaqRevision}><input type="hidden" name="entryId" value={entryId}/><button className="rounded-full border px-3 py-2 text-sm font-semibold">Reject internally</button></form><form action={archiveAdminFaqEntry}><input type="hidden" name="entryId" value={entryId}/><button className="rounded-full border px-3 py-2 text-sm font-semibold">Archive</button></form><form action={restoreAdminFaqEntry}><input type="hidden" name="entryId" value={entryId}/><button className="rounded-full border px-3 py-2 text-sm font-semibold">Restore</button></form></div><p className="mt-3 text-xs text-slate-500">Actions reuse CMS permissions and audit logging. No publish action is exposed.</p></section></div>; }
