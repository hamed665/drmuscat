export type DraftCenterLocationCreateState = {
  ok: boolean;
  message: string | null;
};

export type DraftCenterLocationEditState = {
  ok: boolean;
  message: string | null;
};

export type DraftCenterLocationPrimaryState = {
  ok: boolean;
  message: string | null;
};

export type DraftCenterLocationReviewState = {
  ok: boolean;
  message: string | null;
};
